<resources>

    <!-- Base application theme. -->
    <style name="Theme.Imprint.NoActionbar" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@color/colorWhite</item>
    </style>

    <style name="Theme.Imprint.NoActionbar.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="Theme.Imprint.NoActionbar.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>
</resources>
